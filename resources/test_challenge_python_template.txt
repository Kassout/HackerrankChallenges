import unittest
import filecmp


class {{ class_test_name }}(unittest.TestCase):

    {% for index in outputs -%}
        def test_{{ challenge_name }}_{{ index }}(self):
            solution = "data\\output\\output{{ index }}.txt"
            answer = "output\\output{{ index }}.txt"
            self.assertTrue(filecmp.cmp(solution, answer, shallow=False))

    {% endfor %}

if __name__ == '__main__':
    unittest.main()
